# Build output
build/
.gradle/
*.apk

# Local toolchain - downloaded, not source
.tooling/

# Local SDK path
local.properties

# IDE
.idea/
*.iml

# Study data. Participant material never goes in version control.
study/results/

# Scratch screencaps from emulator runs. Deliberate pitch images live in
# docs/images/ and are committed by name; anything at the root is a working
# file that happened to be lying around during a verification run.
/shot*.png
/*.png

# Local Firebase project configuration
/app/google-services.json
